{
  "watch": ["src/views", "src/styles"],
  "ext": "pug,css",
  "execMap": {
    "pug": "ts-node -P tsconfig.node.json src/pug.ts && npm run copy",
    "css": "npm run copy"
  },
  "exec": "ts-node -P tsconfig.node.json src/pug.ts && npm run copy",
  "ignore": ["src/**/*.ts", "public/**"],
  "delay": "200"
}
